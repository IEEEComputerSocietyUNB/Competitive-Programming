#include<bits/stdc++.h>
using namespace std;

int main() {
    double a, pi = 3.14159;
    cin >> a;
    cout << "VOLUME = " << fixed << setprecision(3) << (4/3.0)*pi*(a*a*a) << endl;
}

//Entrada com variáveis normais e uma saída com quantidade precisa de dígitos
