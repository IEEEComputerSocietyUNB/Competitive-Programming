#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b, c, d, x;
    cin >> a >> b >> c >> d;
    x = (a * b) - (c * d);
    cout << "DIFERENCA = " << x << endl;
    return 0;
}

//Novamente, sem muito mistério aqui: leitura de variáveis na entrada e a operação pretendida final na saída!
